@model GuidModel

<form asp-controller="Restaurant" asp-action="UnarchiveRestaurant" method="Post">
    @Html.TextBoxFor(m => m.Id, new { @type = "hidden", @value = @Model.Id })
    <button type="submit" class="btn btn-outline-secondary btn-sm px-4">Unarchive</button>
</form>